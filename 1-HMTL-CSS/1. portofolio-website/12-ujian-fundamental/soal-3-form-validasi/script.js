// TODO:
// 1. querySelector form dan elemen pesan status
// 2. addEventListener("submit", ...) di form
// 3. e.preventDefault() paling awal di dalam function
// 4. ambil value dari tiap input, .trim() buat username/email
// 5. cek urutan: kosong? → email ada "@"? → password.length >= 6?
//    tiap gagal, set pesan-status jadi pesan error + className "pesan-error", lalu return
// 6. kalau semua lolos, tampilkan pesan sukses + kosongkan semua input
