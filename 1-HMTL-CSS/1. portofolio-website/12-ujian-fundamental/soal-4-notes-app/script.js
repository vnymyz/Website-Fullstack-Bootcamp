// TODO:
// 1. ambilCatatan() → baca localStorage, JSON.parse(), return array (atau [] kalau kosong)
// 2. simpanCatatan(array) → JSON.stringify(array), localStorage.setItem()
// 3. render() → ambilCatatan(), kosongkan <ul>, loop pakai createElement bikin <li> + tombol hapus
// 4. tombol "Tambah Catatan" (dan Enter di input) → push catatan baru, simpanCatatan(), render()
// 5. tombol hapus tiap catatan → filter data buang yang dipilih, simpanCatatan(), render()
// 6. panggil render() sekali di akhir file biar data lama muncul pas halaman dibuka
